# Database (required for production - use Neon, Supabase, or Railway)
# Get these from https://neon.tech (recommended) or your PostgreSQL provider
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
DIRECT_URL="postgresql://user:password@host/database?sslmode=require"

# JWT secret for session signing (required in production - use a long random string)
JWT_SECRET="your-secret-key-min-32-chars"

# Optional: OpenAI API key for Daily Summary tile (get from https://platform.openai.com/api-keys)
# OPENAI_API_KEY="sk-..."

# Optional: Google Calendar integration (see CALENDAR_SETUP.md)
# APP_URL="http://localhost:3000"
# GOOGLE_CLIENT_ID="....apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="..."
